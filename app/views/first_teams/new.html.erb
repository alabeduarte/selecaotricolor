	<%= form_tag Formation.new, :html => {:class => "well form-search"} do |f| %>
		<div class="clearfix">
	  		<div>
				<%= hidden_field_tag 'match_id' %>
				<div class="input">
					<%= select(nil, :id, @matches) %>
				</div>
			</div>
	  	</div>
	<% end %>
	<div class="new_formation_invitation">
		<h2>Tática Oficial</h2>
		<div class="new_formation_actions">
			<%= link_to "Refazer", :new, :class => "btn primary" %>		
			<button id="send_button" name="button" class="btn danger send">Enviar</button>
		</div>
	</div>	
	<div class=".droppable-area">
		<div id="soccerField">
		</div>
		<% if user_signed_in? %>	
		<div id="player">
			<div id="others" >
			</div>
			<h5 class="pills positions"><a href="#">Atacantes</a></h5>		
			<div id="forwards" >
			</div>
			<h5 class="pills positions"><a href="#">Meias</a></h5>		
			<div id="midfield" >
			</div>
			<h5 class="pills positions"><a href="#">Volantes</a></h5>		
			<div id="midfield_defense" >
			</div>
			<h5 class="pills positions"><a href="#">Laterais Esquerdos</a></h5>		
			<div id="left_back" >
			</div>
			<h5 class="pills positions"><a href="#">Zagueiros</a></h5>		
			<div id="defender" >
			</div>
			<h5 class="pills positions"><a href="#">Laterais Direitos</a></h5>		
			<div id="right_back" >
			</div>
			<h5 class="pills positions"><a href="#">Goleiros</a></h5>		
			<div id="goalkeepers" >
			</div>
		</div>
		<% end %>
	<div id="slot"></div>
	</div>
	<div id="matrix"></div>

<%= content_for :javascript do %>
	$( init_soccer_field );
	
	$('#send_button').click(function() {
		if (correctPlayers == 10 && correctGoalKeeper == 1) {
			var json = convertMatrixModelToJson();
			desableSenderButton();			
			$('#send_button').html('Enviando...');
			$.ajax({
		  			url:          "/first_teams",
					dataType:     "json",
					type:         "POST",
					contentType:  "application/json",
					data:         json,
					complete: function() {
						$('#send_button').addClass('success');
						$('#send_button').html('Escalação Enviada!');
					}
			});
		} else {
			cSimpleAlert('Selecione os 11 jogadores!');
		}		  	
	});
	
	
<% end %>