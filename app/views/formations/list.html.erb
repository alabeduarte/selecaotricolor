<h1>Listing Formations</h1>

<table>
  <tr>
    <th>Id</th>
	<th><%= t(:day) %></th>
	<th><%= t(:home) %></th>
	<th><%= t(:away) %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @formations.each do |formation| %>
  <tr>
    <td><%= formation.id %></td>
	<td><%= formation.calendar.day %></td>
	<td><%= formation.calendar.home.name %></td>
	<td><%= formation.calendar.away.name %></td>
    <td><%= link_to 'Show', formation %></td>
    <td><%= link_to 'Edit', edit_formation_path(formation) %></td>
    <td><%= link_to 'Destroy', formation, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Formation', index_path %>
