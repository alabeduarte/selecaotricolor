<% content_for :title, "para #{@formation.match.home.name} x #{@formation.match.away.name}, o meu #{Team.bahia.name} vai de #{@formation.tactical}" %>
<%= render :partial => "show_menu" %>
<div class="hero-unit">
	<h2 class="subtitle"><%= "Seleção Tricolor: para #{@formation.match.home.name} x #{@formation.match.away.name}, o meu #{Team.bahia.name} vai de #{@formation.tactical}" %></h2>
	<%= render :partial => 'facebook_like' %>
	<div id="call_up" class="show">          
		<%= render :partial => "players_holders" %>
	</div>
	<div id="soccerField" class="show">
		<div id="emptySlot"></div>
	</div>		
	<div class="show">          		
	</div>
	<%= render :partial => 'facebook_comments' %>
	<div id="matrix"></div>
	<div id="json"></div>
</div>



<%= content_for :javascript do %>
	$( load_soccer_field );
	$( load_formation );	
<% end %>